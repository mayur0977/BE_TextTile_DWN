export interface Category {
  categoryId: number;
  categoryName: string;
}

export interface CategoryResponseModel {
  categoryId: number;
  categoryName: string;
}
export interface CategoryRequest {
  categoryName: string;
}
